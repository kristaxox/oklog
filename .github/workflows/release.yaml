name: Release

on:
  push:
    branches: [ main ]

jobs:
  container-job:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@v2
      - name: Build binaries
        run: make build
      - name: Build container
        run: make image
